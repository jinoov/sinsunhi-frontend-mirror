// Generated by ReScript, PLEASE EDIT WITH CARE


function encodeSort(sort) {
  if (sort === "UPDATED_DESC") {
    return "updated-desc";
  } else if (sort === "PRICE_ASC") {
    return "price-asc";
  } else if (sort === "PRICE_PER_KG_ASC") {
    return "price-per-kg-asc";
  } else {
    return "";
  }
}

function decodeSort(sectionType, sortStr) {
  switch (sortStr) {
    case "price-desc" :
        return "PRICE_DESC";
    case "price-per-kg-desc" :
        return "PRICE_PER_KG_DESC";
    case "updated-asc" :
        return "UPDATED_ASC";
    case "updated-desc" :
        return "UPDATED_DESC";
    default:
      if (sectionType === "MATCHING") {
        switch (sortStr) {
          case "price-asc" :
          case "price-per-kg-asc" :
              return "PRICE_PER_KG_ASC";
          default:
            
        }
      }
      
  }
  switch (sortStr) {
    case "price-asc" :
    case "price-per-kg-asc" :
        return "PRICE_ASC";
    default:
      return "UPDATED_DESC";
  }
}

function makeSortLabel(sort) {
  if (sort === "UPDATED_DESC") {
    return "최신순";
  } else if (sort === "PRICE_ASC") {
    return "낮은가격순";
  } else if (sort === "PRICE_PER_KG_ASC") {
    return "낮은가격순(kg당)";
  } else {
    return "";
  }
}

var Sort = {
  encodeSort: encodeSort,
  decodeSort: decodeSort,
  makeSortLabel: makeSortLabel
};

function make(s) {
  if (s === undefined) {
    return "ALL";
  }
  switch (s) {
    case "all" :
        return "ALL";
    case "delivery" :
        return "DELIVERY";
    case "matching" :
        return "MATCHING";
    case "none" :
        return "NONE";
    default:
      return "ALL";
  }
}

function toCheckBoxSelection(t) {
  if (t === "DELIVERY") {
    return [
            false,
            true
          ];
  } else if (t === "MATCHING") {
    return [
            true,
            false
          ];
  } else if (t === "NONE") {
    return [
            false,
            false
          ];
  } else {
    return [
            true,
            true
          ];
  }
}

function fromCheckBoxSelection(matching, delivery) {
  if (matching) {
    if (delivery) {
      return "ALL";
    } else {
      return "MATCHING";
    }
  } else if (delivery) {
    return "DELIVERY";
  } else {
    return "NONE";
  }
}

function toString(t) {
  if (t === "DELIVERY") {
    return "delivery";
  } else if (t === "MATCHING") {
    return "matching";
  } else if (t === "NONE") {
    return "none";
  } else {
    return "all";
  }
}

function toQueryParam(t) {
  if (t === "DELIVERY") {
    return [
            "NORMAL",
            "QUOTABLE"
          ];
  } else if (t === "MATCHING") {
    return [
            "MATCHING",
            "QUOTED"
          ];
  } else if (t === "NONE") {
    return [];
  } else {
    return [
            "MATCHING",
            "QUOTED",
            "NORMAL",
            "QUOTABLE"
          ];
  }
}

var Section = {
  make: make,
  toCheckBoxSelection: toCheckBoxSelection,
  fromCheckBoxSelection: fromCheckBoxSelection,
  toString: toString,
  toQueryParam: toQueryParam
};

export {
  Sort ,
  Section ,
}
/* No side effect */
