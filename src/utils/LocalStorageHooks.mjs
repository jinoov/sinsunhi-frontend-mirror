// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Spice from "@greenlabs/ppx-spice/src/rescript/Spice.mjs";
import * as Belt_Option from "rescript/lib/es6/belt_Option.js";
import * as Belt_Result from "rescript/lib/es6/belt_Result.js";
import * as LocalStorage from "./LocalStorage.mjs";

function fromString(str) {
  return Belt_Option.getWithDefault(str, "");
}

function toString(value) {
  return value;
}

var PhoneNumberConfig = {
  key: "SS_PHONENUMBER",
  fromString: fromString,
  toString: toString
};

function fromString$1(str) {
  return Belt_Option.getWithDefault(str, "");
}

function toString$1(value) {
  return value;
}

var BuyerEmailConfig = {
  key: "SS_BUYER_EMAIL",
  fromString: fromString$1,
  toString: toString$1
};

function fromString$2(str) {
  return Belt_Option.getWithDefault(str, "");
}

function toString$2(value) {
  return value;
}

var EmailAdminConfig = {
  key: "SS_EMAIL_ADMIN",
  fromString: fromString$2,
  toString: toString$2
};

function t_encode(v) {
  return Spice.arrayToJson(Spice.stringToJson, v);
}

function t_decode(v) {
  return Spice.arrayFromJson(Spice.stringFromJson, v);
}

var key = "SS_ADMIN_MENU";

function fromString$3(str) {
  try {
    return Belt_Option.mapWithDefault(str, [], (function (str$p) {
                  var v = JSON.parse(str$p);
                  return Belt_Result.getWithDefault(Spice.arrayFromJson(Spice.stringFromJson, v), []);
                }));
  }
  catch (exn){
    return [];
  }
}

function toString$3(arr) {
  return JSON.stringify(Spice.arrayToJson(Spice.stringToJson, arr));
}

var AdminMenuConfig = {
  t_encode: t_encode,
  t_decode: t_decode,
  key: key,
  fromString: fromString$3,
  toString: toString$3
};

function fromString$4(str) {
  return Belt_Option.getWithDefault(str, "");
}

function toString$4(value) {
  return value;
}

var AccessTokenConfig = {
  key: "SS_ACCESS_TOKEN",
  fromString: fromString$4,
  toString: toString$4
};

function fromString$5(str) {
  return Belt_Option.getWithDefault(str, "");
}

function toString$5(value) {
  return value;
}

var RefreshTokenConfig = {
  key: "SS_REFRESH_TOKEN",
  fromString: fromString$5,
  toString: toString$5
};

function fromString$6(str) {
  return Belt_Option.getWithDefault(str, "");
}

function toString$6(value) {
  return value;
}

var BuyerInfoConfig = {
  key: "SS_BUYER_INFO_LAST_SHOWN",
  fromString: fromString$6,
  toString: toString$6
};

var PhoneNumber = LocalStorage.Make(PhoneNumberConfig);

var BuyerEmail = LocalStorage.Make(BuyerEmailConfig);

var EmailAdmin = LocalStorage.Make(EmailAdminConfig);

var AdminMenu = LocalStorage.Make({
      key: key,
      fromString: fromString$3,
      toString: toString$3
    });

var AccessToken = LocalStorage.Make(AccessTokenConfig);

var RefreshToken = LocalStorage.Make(RefreshTokenConfig);

var BuyerInfoLastShown = LocalStorage.Make(BuyerInfoConfig);

export {
  PhoneNumberConfig ,
  BuyerEmailConfig ,
  EmailAdminConfig ,
  AdminMenuConfig ,
  AccessTokenConfig ,
  RefreshTokenConfig ,
  BuyerInfoConfig ,
  PhoneNumber ,
  BuyerEmail ,
  EmailAdmin ,
  AdminMenu ,
  AccessToken ,
  RefreshToken ,
  BuyerInfoLastShown ,
}
/* PhoneNumber Not a pure module */
