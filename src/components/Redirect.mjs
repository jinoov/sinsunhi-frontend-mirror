// Generated by ReScript, PLEASE EDIT WITH CARE

import * as React from "react";
import * as Belt_Array from "rescript/lib/es6/belt_Array.js";
import * as Belt_Option from "rescript/lib/es6/belt_Option.js";
import * as Caml_option from "rescript/lib/es6/caml_option.js";

function setHref(path) {
  Belt_Option.map(Caml_option.undefined_to_opt(typeof window === "undefined" ? undefined : window), (function (param) {
          window.location.href = path;
        }));
}

function redirectByRole(param) {
  var currentPathname = window.location.pathname;
  var firstPath = Belt_Array.get(Belt_Array.keep(currentPathname.split("/"), (function (x) {
              return x !== "";
            })), 0);
  var tmp;
  var exit = 0;
  if (firstPath !== undefined) {
    switch (firstPath) {
      case "admin" :
          tmp = "/admin/signin?redirect=" + currentPathname + "";
          break;
      case "buyer" :
          exit = 1;
          break;
      case "seller" :
          tmp = "/seller/signin?redirect=" + currentPathname + "";
          break;
      default:
        exit = 1;
    }
  } else {
    exit = 1;
  }
  if (exit === 1) {
    tmp = "/buyer/signin?redirect=" + currentPathname + "";
  }
  setHref(tmp);
}

function Redirect(props) {
  var message = props.message;
  var path = props.path;
  React.useEffect((function () {
          setHref(path);
        }), []);
  return React.createElement("div", undefined, message !== undefined ? message : React.createElement("div", {
                    className: "container mx-auto flex justify-center items-center h-screen"
                  }, "페이지 전환 중 입니다."));
}

var make = Redirect;

export {
  setHref ,
  redirectByRole ,
  make ,
}
/* react Not a pure module */
