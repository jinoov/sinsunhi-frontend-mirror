// Generated by ReScript, PLEASE EDIT WITH CARE

import * as React from "react";
import * as ErrorPanel from "./common/ErrorPanel.mjs";
import * as CustomHooks from "../utils/CustomHooks.mjs";
import * as Router from "next/router";
import * as Download_Center_Table from "./Download_Center_Table.mjs";

function DownloadCenter_Render(Props) {
  var children = Props.children;
  var router = Router.useRouter();
  var status = CustomHooks.Downloads.use(new URLSearchParams(router.query).toString());
  if (typeof status === "number") {
    return React.createElement("div", undefined, "로딩 중..");
  }
  if (status.TAG !== /* Loaded */0) {
    return React.createElement(ErrorPanel.make, {
                error: status._0
              });
  }
  var decode = CustomHooks.Downloads.downloads_decode(status._0);
  if (decode.TAG === /* Ok */0) {
    return React.cloneElement(children, {
                children: React.createElement(Download_Center_Table.make, {
                      "downloads'": decode._0
                    })
              });
  } else {
    return React.createElement("div", undefined, "오류가 발생하였습니다.");
  }
}

var make = DownloadCenter_Render;

export {
  make ,
}
/* react Not a pure module */
