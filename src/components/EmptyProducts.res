@react.component
let make = () => {
  <div className=%twc("grid col-span-full justify-center items-center text-black-gl h-96")>
    <div className=%twc("")> {j`단품이 없습니다.`->React.string} </div>
  </div>
}
